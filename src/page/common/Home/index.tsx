import { SvgIcon } from '@Core/helpers';

const Home = () => {
   return (
      <>
         <button>
            hello
            <SvgIcon name="react" />
         </button>
      </>
   );
};

export default Home;
